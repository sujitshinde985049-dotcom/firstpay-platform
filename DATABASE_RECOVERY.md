# Database Recovery

Declare the incident, stop writes when integrity is uncertain, preserve logs, select a verified restore point, restore into an isolated project, run migrations and integrity checks, then cut over only with incident approval. Validate authentication, RLS, tenant counts, financial references, webhook idempotency, Storage metadata, and audit continuity. Rotate credentials after exposure and document RPO/RTO results.
