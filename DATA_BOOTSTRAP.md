# Data Bootstrap

Development and staging may use separate, clearly labelled seed scripts with mock users and `MOCK ONLY` transactions. Production bootstrap applies migrations and essential provider/plan/platform configuration only—never users, passwords, testimonials, transactions, KYC, provider credentials, or customer data. Review every seed before execution and record environment, commit, operator, and result.
