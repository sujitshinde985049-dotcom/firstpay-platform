# Backup and Recovery

Back up Supabase database and private Storage under approved retention/encryption controls; preserve migration history, environment-variable inventory (not values), and credential rotation procedures. Test restore, failed-deployment rollback, provider outage fail-closed behavior, data export, and incident communication. Never store dumps or secrets in GitHub.
