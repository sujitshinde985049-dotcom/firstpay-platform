# Domain Setup

Verify domain ownership in Vercel, configure DNS, enforce HTTPS, select one canonical host, update `NEXT_PUBLIC_APP_URL`, metadata, sitemap, Supabase Site URL and exact Auth redirect allowlist, email links, webhook registrations, CSP exceptions, and monitoring. Test apex/`www` redirects and retain DNS rollback records.
