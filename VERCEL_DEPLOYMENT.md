# Vercel Deployment

Import the GitHub repository, keep Next.js framework detection, Node 20+, `npm ci`, and `npm run build`. Configure Preview and Production variables independently, set the canonical application URL, add exact Supabase callback URLs, and validate CSP/connect-src exceptions before promotion. Preview deployments must use non-production Supabase and provider sandbox configuration. Production deployment requires reviewed PR, migration approval, smoke tests, and rollback readiness.

This task did not deploy the application.
